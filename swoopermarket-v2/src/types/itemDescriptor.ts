export type Descriptor = {
    listingTitle: string,
    sellerId: number,
    Name: string,
    email: string,
    phone: string,
    description: string,
    price: number,
    condition: string,
    pickup: string
}
